---
title: "Azure에서 온-프레미스로 VMware VM 장애 복구 | Microsoft Docs"
description: "VMware VM 및 물리적 서버를 Azure로 장애 조치한 후 온-프레미스 사이트로 장애 복구에 대해 알아봅니다."
services: site-recovery
documentationcenter: 
author: ruturaj
manager: mkjain
editor: 
ms.assetid: 5a47337f-89a9-43e8-8fdc-7da373c0fb0f
ms.service: site-recovery
ms.devlang: na
ms.tgt_pltfrm: na
ms.topic: article
ms.workload: storage-backup-recovery
ms.date: 03/27/2017
ms.author: ruturajd
ms.openlocfilehash: dde0bb6b4f6bc10afdd7d40adc6689d42b37de81
ms.sourcegitcommit: 02e69c4a9d17645633357fe3d46677c2ff22c85a
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 08/03/2017
---
# <a name="fail-back-vmware-virtual-machines-and-physical-servers-to-the-on-premises-site"></a><span data-ttu-id="036b3-103">온-프레미스 사이트로 VMWare 가상 컴퓨터 및 물리적 서버 장애 복구</span><span class="sxs-lookup"><span data-stu-id="036b3-103">Fail back VMware virtual machines and physical servers to the on-premises site</span></span>


<span data-ttu-id="036b3-104">이 문서에서는 Azure 가상 컴퓨터를 Azure에서 온-프레미스 사이트로 장애 복구하는 방법에 대해 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-104">This article describes how to failback Azure virtual machines from Azure to the on-premises site.</span></span> <span data-ttu-id="036b3-105">이 [참조](site-recovery-how-to-reprotect.md)를 사용하여 컴퓨터를 다시 보호한 후에는 VMware 가상 컴퓨터 또는 Windows/Linux 물리적 서버를 장애 복구할 준비가 될 때 여기에 나온 지침을 따릅니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-105">Follow the instructions here when you're ready to fail back your VMware virtual machines or Windows or Linux physical servers after you have re-protected your machines using this [reference](site-recovery-how-to-reprotect.md).</span></span>

>[!NOTE]
><span data-ttu-id="036b3-106">클래식 Azure Portal을 사용하는 경우 VMware에서 Azure로 아키텍처를 강화하기 위해서는 [여기](site-recovery-failback-azure-to-vmware-classic.md)의 지침을 따르고, 기존 아키텍처를 사용하려면 [여기](site-recovery-failback-azure-to-vmware-classic-legacy.md)를 참조하세요.</span><span class="sxs-lookup"><span data-stu-id="036b3-106">If you are using the classic Azure portal, please refer to instructions mentioned [here](site-recovery-failback-azure-to-vmware-classic.md) for enhanced VMware to Azure architecture and [here](site-recovery-failback-azure-to-vmware-classic-legacy.md) for the legacy architecture</span></span>

## <a name="overview"></a><span data-ttu-id="036b3-107">개요</span><span class="sxs-lookup"><span data-stu-id="036b3-107">Overview</span></span>
<span data-ttu-id="036b3-108">이 섹션의 다이어그램에서는 이 시나리오의 장애 복구 아키텍처를 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-108">The diagrams in this section show the failback architecture for this scenario.</span></span>

<span data-ttu-id="036b3-109">프로세스 서버가 온-프레미스이고 ExpressRoute 연결을 사용하는 경우 이 아키텍처를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-109">When the Process Server is on-premises and you are using an Azure ExpressRoute connection, use this architecture:</span></span>

![ExpressRoute 아키텍처 다이어그램](./media/site-recovery-failback-azure-to-vmware-classic/architecture.png)

<span data-ttu-id="036b3-111">프로세스 서버가 Azure에 있고 VPN 또는 ExpressRoute 연결을 사용하는 경우 이 아키텍처를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-111">When the Process Server is on Azure and you have either a VPN or an ExpressRoute connection, use this architecture:</span></span>

![VPN 아키텍처 다이어그램](./media/site-recovery-failback-azure-to-vmware-classic/architecture2.png)

<span data-ttu-id="036b3-113">포트 목록 전체 및 장애 복구 아키텍처 다이어그램을 보려면 다음 이미지를 참조하세요.</span><span class="sxs-lookup"><span data-stu-id="036b3-113">For a complete list of ports and the failback architecture diagram, refer to the following image:</span></span>

![모든 포트의 장애 조치-장애 복구 목록](./media/site-recovery-failback-azure-to-vmware-classic/Failover-Failback.png)

<span data-ttu-id="036b3-115">Azure로 장애 조치한 후 다음 세 단계로 온-프레미스 사이트에 장애 복구합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-115">After you’ve failed over to Azure, you fail back to your on-premises site in three stages:</span></span>

* <span data-ttu-id="036b3-116">**1단계**: Azure VM을 다시 보호하여 온-프레미스 사이트에서 실행 중인 VMware VM으로 복제를 다시 시작합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-116">**Stage 1**: You reprotect the Azure VMs so that they start replicating back to the VMware VMs that are running on your on-premises site.</span></span>
* <span data-ttu-id="036b3-117">**2단계**: Azure VM이 온-프레미스 사이트에 복제된 후 Azure에서 장애 복구하도록 장애 조치를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-117">**Stage 2**: After your Azure VMs are replicated to your on-premises site, you run a failover to fail back from Azure.</span></span>
* <span data-ttu-id="036b3-118">**3단계**: 데이터가 장애 복구된 후 Azure로 복제가 시작하도록 장애 복구한 온-프레미스 VM을 다시 보호합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-118">**Stage 3**: After your data has failed back, you reprotect the on-premises VMs that you failed back to, so that they start replicating to Azure.</span></span>

### <a name="fail-back-to-the-original-location-or-an-alternate-location"></a><span data-ttu-id="036b3-119">원래 위치 또는 다른 위치로 장애 복구</span><span class="sxs-lookup"><span data-stu-id="036b3-119">Fail back to the original location or an alternate location</span></span>
<span data-ttu-id="036b3-120">VMware VM을 장애 조치한 후에도 동일한 원본 VM이 여전히 온-프레미스에 있는 경우 이 원본 VM으로 장애 복구할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-120">After you fail over a VMware VM, you can fail back to the same source VM if it still exists on-premises.</span></span> <span data-ttu-id="036b3-121">이 시나리오에서는 델타만 장애 복구합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-121">In this scenario, only the deltas are failed back.</span></span>

<span data-ttu-id="036b3-122">물리적 서버를 장애 조치한 경우 장애 복구는 항상 새 VMware VM으로 수행됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-122">If you failed over physical servers, failback is always to a new VMware VM.</span></span> <span data-ttu-id="036b3-123">물리적 컴퓨터를 장애 복구하기 전에 다음 사항에 유의하세요.</span><span class="sxs-lookup"><span data-stu-id="036b3-123">Before failing back a physical machine, note that:</span></span>
* <span data-ttu-id="036b3-124">보호된 물리적 컴퓨터가 Azure에서 VMware로 다시 장애 조치된 경우 가상 컴퓨터로 다시 돌아옵니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-124">A protected physical machine will come back as a virtual machine when it is failed over back from Azure to VMware.</span></span> <span data-ttu-id="036b3-125">Windows Server 2008 R2 SP1 물리적 컴퓨터가 보호되고 Azure로 장애 조치된 경우 장애 복구될 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-125">A Windows Server 2008 R2 SP1 physical machine, if it is protected and failed over to Azure, cannot be failed back.</span></span> <span data-ttu-id="036b3-126">온-프레미스 가상 컴퓨터로 시작된 Windows Server 2008 R2 SP1 컴퓨터는 장애 복구할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-126">A Windows Server 2008 R2 SP1 machine that started as a virtual machine on-premises will be able to fail back.</span></span>
* <span data-ttu-id="036b3-127">장애 복구에 필요한 필수 ESX/ESXi 호스트와 함께 하나 이상의 마스터 대상 서버를 검색해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-127">Ensure that you discover at least one master target server along with the necessary ESX/ESXi hosts that you need to fail back to.</span></span>

<span data-ttu-id="036b3-128">원래 VM으로 장애 복구하는 경우 다음이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-128">If you fail back to the original VM, the following are required:</span></span>

* <span data-ttu-id="036b3-129">VM이 vCenter 서버에서 관리되는 경우 마스터 대상의 ESX 호스트에서 VM 데이터 저장소에 액세스할 수 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-129">If the VM is managed by a vCenter server, the master target's ESX host should have access to the VMs datastore.</span></span>
* <span data-ttu-id="036b3-130">VM이 ESX 호스트에 있지만 vCenter에서 관리되지 않는 경우 VM의 하드 디스크는 MT의 호스트에서 액세스할 수 있는 데이터 저장소에 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-130">If the VM is on an ESX host but isn’t managed by vCenter, the hard disk of the VM must be in a datastore that's accessible by the MT's host.</span></span>
* <span data-ttu-id="036b3-131">VM이 ESX 호스트에 있고 vCenter를 사용하지 않는 경우 다시 보호하기 전에 MT의 ESX 호스트 검색을 완료해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-131">If your VM is on an ESX host and doesn't use vCenter, you should complete discovery of the ESX host of the MT before you reprotect.</span></span> <span data-ttu-id="036b3-132">물리적 서버도 장애 복구하는 경우 적용됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-132">This applies if you're failing back physical servers too.</span></span>
* <span data-ttu-id="036b3-133">다른 옵션(온-프레미스 VM이 있는 경우)은 장애 복구를 수행하기 전에 삭제하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-133">Another option (if the on-premises VM exists) is to delete it before you do a failback.</span></span> <span data-ttu-id="036b3-134">그런 다음 장애 복구를 수행하면 마스터 대상 ESX 호스트와 동일한 호스트에 새 VM을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-134">Failback then creates a new VM on the same host as the master target ESX host.</span></span>

<span data-ttu-id="036b3-135">대체 위치로 장애 복구하는 경우 온-프레미스 마스터 대상 서버에서 사용한 것과 동일한 데이터 저장소와 동일한 ESX 호스트에 데이터를 복구합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-135">When you fail back to an alternate location, the data is recovered to the same datastore and the same ESX host as that used by the on-premises master target server.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="036b3-136">필수 조건</span><span class="sxs-lookup"><span data-stu-id="036b3-136">Prerequisites</span></span>
* <span data-ttu-id="036b3-137">VMware VM 및 물리적 서버를 장애 복구하려면 VMware 환경이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-137">To fail back VMware VMs and physical servers, you need a VMware environment.</span></span> <span data-ttu-id="036b3-138">물리적 서버로 장애 복구는 지원되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-138">Failing back to a physical server isn’t supported.</span></span>
* <span data-ttu-id="036b3-139">장애 복구하려면 보호를 처음 설정할 때 Azure 네트워크를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-139">To fail back, you must have created an Azure network when you initially set up protection.</span></span> <span data-ttu-id="036b3-140">장애 복구는 온-프레미스 사이트에 있는 Azure VM이 있는 Azure 네트워크에서 VPN 또는 Express 경로 연결이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-140">Failback needs a VPN or ExpressRoute connection from the Azure network in which the Azure VMs are located to the on-premises site.</span></span>
* <span data-ttu-id="036b3-141">장애 복구하려는 VM을 vCenter Server에서 관리하는 경우 vCenter 서버에서 VM을 검색하는 데 필요한 권한이 있는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-141">If the VMs that you want to fail back to are managed by a vCenter server, make sure that you have the required permissions for discovery of VMs on vCenter servers.</span></span> <span data-ttu-id="036b3-142">자세한 내용은 [Azure Site Recovery를 사용하여 Azure에 VMware 가상 컴퓨터 및 물리적 서버 복제](site-recovery-vmware-to-azure-classic.md)를 참조하세요.</span><span class="sxs-lookup"><span data-stu-id="036b3-142">For more information, see [Replicate VMware virtual machines and physical servers to Azure with Azure Site Recovery](site-recovery-vmware-to-azure-classic.md).</span></span>
* <span data-ttu-id="036b3-143">스냅숏이 VM에 있는 경우 다시 보호에 실패합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-143">If snapshots are present on a VM, reprotection fails.</span></span> <span data-ttu-id="036b3-144">스냅숏 또는 디스크를 삭제할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-144">You can delete the snapshots or the disks.</span></span>
* <span data-ttu-id="036b3-145">장애 복구하기 전에 다음 구성 요소를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-145">Before you fail back, create these components:</span></span>
  * <span data-ttu-id="036b3-146">**Azure에 프로세스 서버 만들기**:</span><span class="sxs-lookup"><span data-stu-id="036b3-146">**Create a Process Server in Azure**.</span></span> <span data-ttu-id="036b3-147">이 구성 요소는 장애 복구 중에 만들고 계속 실행하는 Azure VM입니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-147">This component is an Azure VM that you create and keep running during failback.</span></span> <span data-ttu-id="036b3-148">장애 복구가 완료되면 VM을 삭제할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-148">You can delete the VM after failback is complete.</span></span>
  * <span data-ttu-id="036b3-149">**마스터 대상 서버 만들기**: 마스터 대상 서버는 장애 복구 데이터를 송수신합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-149">**Create a master target server**: The master target server sends and receives failback data.</span></span> <span data-ttu-id="036b3-150">온-프레미스를 만든 관리 서버에는 기본적으로 설치된 마스터 대상 서버가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-150">The management server that you created on-premises has a master target server that's installed by default.</span></span> <span data-ttu-id="036b3-151">그러나 장애 복구된 트래픽의 양에 따라 장애 복구를 위한 별도의 마스터 대상 서버를 만들어야 할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-151">However, depending on the volume of failed-back traffic, you might need to create a separate master target server for failback.</span></span>
  * <span data-ttu-id="036b3-152">Linux에서 실행되는 추가 마스터 대상 서버를 만들려면 나중에 설명하는 대로 마스터 대상 서버를 설치하기 전에 Linux VM을 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-152">To create an additional master target server that runs on Linux, set up the Linux VM before you install the master target server, as described later.</span></span>
* <span data-ttu-id="036b3-153">장애 복구를 수행할 때 구성 서버가 온-프레미스에 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-153">A configuration server is required on-premises when you do a failback.</span></span> <span data-ttu-id="036b3-154">장애 복구하는 동안 가상 컴퓨터가 구성 서버 데이터베이스에 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-154">During failback, the virtual machine must exist in the configuration server database.</span></span> <span data-ttu-id="036b3-155">구성 서버 데이터베이스에 VM이 없으면 장애 복구가 성공할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-155">If the configuration server database contains no VM, failback cannot succeed.</span></span> <span data-ttu-id="036b3-156">따라서 서버의 예약된 정기 백업을 수행해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-156">Therefore, ensure that you make regular scheduled backups of your server.</span></span> <span data-ttu-id="036b3-157">재난이 발생하면 장애 복구가 작동할 수 있도록 동일한 IP 주소로 복원해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-157">In a disaster, you need to restore it with the same IP address so that failback works.</span></span>
* <span data-ttu-id="036b3-158">VMware의 마스터 대상 VM **구성 매개 변수**에서 disk.enableUUID=true로 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-158">Set the disk.enableUUID=true setting in **Configuration Parameters** of the master target VM in VMware.</span></span> <span data-ttu-id="036b3-159">이 행이 존재하지 않는 경우 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-159">If this row does not exist, add it.</span></span> <span data-ttu-id="036b3-160">이 설정은 가상 컴퓨터 디스크(VMDK) 파일에 일관된 UUID(Universally Unique Identifier)를 제공하여 올바르게 탑재되도록 하는데 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-160">This setting is required to provide a consistent universally unique identifier (UUID) to the virtual machine disk (VMDK) file so that it is mounted correctly.</span></span>
* <span data-ttu-id="036b3-161">장애 복구 실패가 발생할 수 있는 "마스터 대상 서버는 vMotioned 저장소가 될 수 없음" 조건을 알고 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-161">Be aware of a "Master target server cannot be storage vMotioned" condition, which can cause the failback to fail.</span></span> <span data-ttu-id="036b3-162">디스크를 사용할 수 없으므로 VM을 시작할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-162">The VM cannot come up because the disks aren't made available to it.</span></span>
* <span data-ttu-id="036b3-163">마스터 대상 서버에 보존 드라이브라고 하는 드라이브를 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-163">Add a drive, called a retention drive, onto the master target server.</span></span> <span data-ttu-id="036b3-164">디스크를 추가하고 드라이브를 포맷합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-164">Add a disk, and format the drive.</span></span>

## <a name="failback-policy"></a><span data-ttu-id="036b3-165">장애 복구 정책</span><span class="sxs-lookup"><span data-stu-id="036b3-165">Failback policy</span></span>
<span data-ttu-id="036b3-166">온-프레미스로 다시 복제하려면 장애 복구 정책이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-166">To replicate back to on-premises, you need a failback policy.</span></span> <span data-ttu-id="036b3-167">정책은 정방향 정책을 만들 때 자동으로 만들어지고, 구성 서버와 자동으로 연결되며,</span><span class="sxs-lookup"><span data-stu-id="036b3-167">The policy is automatically created when you create a forward direction policy, and it is automatically associated with the configuration server.</span></span> <span data-ttu-id="036b3-168">편집할 수는 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-168">It is not editable.</span></span> <span data-ttu-id="036b3-169">정책의 복제 설정은 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-169">The policy has the following replication settings:</span></span>

* <span data-ttu-id="036b3-170">RPO 임계값 = 15분</span><span class="sxs-lookup"><span data-stu-id="036b3-170">RPO threshold = 15 minutes</span></span>
* <span data-ttu-id="036b3-171">복구 지점 보존 기간 = 24시간</span><span class="sxs-lookup"><span data-stu-id="036b3-171">Recovery point retention = 24 hours</span></span>
* <span data-ttu-id="036b3-172">앱 일치 스냅숏 빈도 = 60분</span><span class="sxs-lookup"><span data-stu-id="036b3-172">App consistent snapshot frequency = 60 minutes</span></span>

 ![장애 복구 정책의 복제 설정](./media/site-recovery-failback-azure-to-vmware-new/failback-policy.png)

## <a name="set-up-a-process-server-in-azure"></a><span data-ttu-id="036b3-174">Azure에서 프로세스 서버 설정</span><span class="sxs-lookup"><span data-stu-id="036b3-174">Set up a Process Server in Azure</span></span>
<span data-ttu-id="036b3-175">Azure VM에서 데이터를 온-프레미스 마스터 대상 서버로 다시 보낼 수 있도록 Azure에 프로세스 서버를 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-175">Install a Process Server in Azure so that the Azure VMs can send the data back to the on-premises master target server.</span></span>

<span data-ttu-id="036b3-176">클래식 리소스로서 컴퓨터를 보호한 경우(Azure에서 복구되는 VM이 클래식 배포 모델을 사용하여 만들어진 VM이기 때문) Azure에 프로세스 서버가 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-176">If you have protected your virtual machines as classic resources (that is, the VM recovered in Azure is a VM that was created by using the classic deployment model), you need a Process Server in Azure.</span></span> <span data-ttu-id="036b3-177">배포 유형으로 Azure Resource Manager를 사용하여 VM을 복구한 경우 프로세스 서버에는 배포 유형으로 Resource Manager가 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-177">If you have recovered the VMs with Azure Resource Manager as the deployment type, the Process Server must have Resource Manager as the deployment type.</span></span> <span data-ttu-id="036b3-178">프로세스 서버를 배포하는 Azure 가상 네트워크에서 배포 유형을 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-178">The deployment type is selected by the Azure virtual network that you deploy the Process Server to.</span></span>

1. <span data-ttu-id="036b3-179">**자격 증명 모음** > **설정** > **Site Recovery 인프라**(**관리** 아래) > **구성 서버**(**VMware 및 물리적 컴퓨터**)에서 구성 서버를 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-179">In **Vault** > **Settings** > **Site Recovery Infrastructure** (under **Manage**) > **Configuration Servers** (under **For VMware and Physical Machines**), select the configuration server.</span></span>
2. <span data-ttu-id="036b3-180">**프로세스 서버**를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-180">Click **Process Server**.</span></span>

  ![프로세스 서버 단추](./media/site-recovery-failback-azure-to-vmware-classic/add-processserver.png)
3. <span data-ttu-id="036b3-182">프로세스 서버 배포를 위해 **Azure에서 장애 복구 프로세스 서버 배포**를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-182">Choose to deploy the Process Server as **Deploy a failback Process Server in Azure**.</span></span>
4. <span data-ttu-id="036b3-183">VM을 복구한 구독을 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-183">Select the subscription that you have recovered the VMs to.</span></span>
5. <span data-ttu-id="036b3-184">VM을 복구한 Azure 네트워크를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-184">Select the Azure network that you have recovered the VMs to.</span></span> <span data-ttu-id="036b3-185">복구된 VM과 프로세스 서버가 통신할 수 있도록 프로세스 서버는 동일한 네트워크에 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-185">The Process Server needs to be in the same network so that the recovered VMs and the Process Server can communicate.</span></span>
6. <span data-ttu-id="036b3-186">*클래식 배포 모델* 네트워크를 선택한 경우 Azure Marketplace를 통해 VM을 만든 다음 그 안에 프로세스 서버를 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-186">If you selected a *classic deployment model* network, create a VM via the Azure Marketplace, and then install the Process Server in it.</span></span>

 !["프로세스 서버 추가" 창](./media/site-recovery-failback-azure-to-vmware-classic/add-classic.png)

 <span data-ttu-id="036b3-188">프로세스 서버를 만들 때 다음 사항에 주의하세요.</span><span class="sxs-lookup"><span data-stu-id="036b3-188">As you're creating the Process Server, pay attention to the following:</span></span>
 * <span data-ttu-id="036b3-189">이미지 이름은 *Microsoft Azure Site Recovery 프로세스 서버 V2*입니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-189">The name of the image is *Microsoft Azure Site Recovery Process Server V2*.</span></span> <span data-ttu-id="036b3-190">배포 모델로 **클래식**을 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-190">Select **Classic** as the deployment model.</span></span>

       ![Select "Classic" as the Process Server deployment model](./media/site-recovery-failback-azure-to-vmware-classic/templatename.png)
 * <span data-ttu-id="036b3-191">[Azure Site Recovery를 사용하여 Azure에 VMware 가상 컴퓨터 및 물리적 서버 복제](site-recovery-vmware-to-azure-classic.md)의 지침에 따라 프로세스 서버를 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-191">Install the Process Server according to the instructions in [Replicate VMware virtual machines and physical servers to Azure with Azure Site Recovery](site-recovery-vmware-to-azure-classic.md).</span></span>
7. <span data-ttu-id="036b3-192">*Resource Manager* Azure 네트워크를 선택하는 경우 다음 정보를 제공하여 프로세스 서버를 배포합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-192">If you select the *Resource Manager* Azure network, deploy the Process Server by providing the following information:</span></span>

  * <span data-ttu-id="036b3-193">서버를 배포하려는 리소스 그룹 이름</span><span class="sxs-lookup"><span data-stu-id="036b3-193">The name of the resource group that you want to deploy the server to</span></span>
  * <span data-ttu-id="036b3-194">서버 이름</span><span class="sxs-lookup"><span data-stu-id="036b3-194">The name of the server</span></span>
  * <span data-ttu-id="036b3-195">서버에 로그인하기 위한 사용자 이름과 암호</span><span class="sxs-lookup"><span data-stu-id="036b3-195">A username and password for signing in to the server</span></span>
  * <span data-ttu-id="036b3-196">서버를 배포하려는 저장소 계정</span><span class="sxs-lookup"><span data-stu-id="036b3-196">The storage account that you want to deploy the server to</span></span>
  * <span data-ttu-id="036b3-197">연결하려는 서브넷 및 네트워크 인터페이스</span><span class="sxs-lookup"><span data-stu-id="036b3-197">The subnet and the network interface that you want to connect to it</span></span>
   >[!NOTE]
   ><span data-ttu-id="036b3-198">프로세스 서버를 배포하는 중에 사용자 고유의 [NIC(네트워크 인터페이스)](../virtual-network/virtual-networks-multiple-nics.md)를 만들고 선택해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-198">You must create your own [network interface](../virtual-network/virtual-networks-multiple-nics.md) (NIC) and select it while you are deploying the Process Server.</span></span>

    !["프로세스 서버 추가" 대화 상자에 정보 입력](./media/site-recovery-failback-azure-to-vmware-classic/psinputsadd.png)

8. <span data-ttu-id="036b3-200">**확인**을 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-200">Click **OK**.</span></span> <span data-ttu-id="036b3-201">이 작업은 프로세스 서버 설치 중에 Resource Manager 배포 유형 가상 컴퓨터를 만드는 작업을 트리거합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-201">This action triggers a job that creates a Resource Manager deployment type virtual machine during the Process Server setup.</span></span> <span data-ttu-id="036b3-202">서버를 구성 서버에 등록하려면 [Azure Site Recovery를 사용하여 Azure에 VMware 가상 컴퓨터 및 물리적 서버 복제](site-recovery-vmware-to-azure-classic.md)의 지침에 따라 VM 내에서 설정을 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-202">To register the server to the configuration server, run the setup inside the VM by following the instructions in [Replicate VMware virtual machines and physical servers to Azure with Azure Site Recovery](site-recovery-vmware-to-azure-classic.md).</span></span> <span data-ttu-id="036b3-203">프로세스 서버를 배포하는 작업도 트리거합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-203">A job to deploy the Process Server is also triggered.</span></span>

  <span data-ttu-id="036b3-204">프로세스 서버가 **구성 서버** > **연결된 서버** > **프로세스 서버** 탭에 나열됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-204">The Process Server is listed on the **Configuration servers** > **Associated servers** > **Process Servers** tab.</span></span>

    ![](./media/site-recovery-failback-azure-to-vmware-new/pslistingincs.png)

    > [!NOTE]
    > <span data-ttu-id="036b3-205">프로세스 서버는 **VM 속성** 아래에 표시되지 않으며,</span><span class="sxs-lookup"><span data-stu-id="036b3-205">The Process Server isn't visible under **VM properties**.</span></span> <span data-ttu-id="036b3-206">등록된 관리 서버의 **서버** 탭에서만 볼 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-206">It's visible only on the **Servers** tab in the management server that it's registered to.</span></span> <span data-ttu-id="036b3-207">프로세스 서버가 나타날 때까지 10-15분이 걸릴 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-207">It can take 10 to 15 minutes for the Process Server to appear.</span></span>


## <a name="set-up-the-master-target-server-on-premises"></a><span data-ttu-id="036b3-208">마스터 대상 서버 온-프레미스 설정</span><span class="sxs-lookup"><span data-stu-id="036b3-208">Set up the master target server on-premises</span></span>
<span data-ttu-id="036b3-209">마스터 대상 서버는 장애 복구 데이터를 받습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-209">The master target server receives the failback data.</span></span> <span data-ttu-id="036b3-210">서버가 온-프레미스 관리 서버에 자동으로 설치되지만 너무 많은 데이터를 장애 복구하는 경우 추가 마스터 대상 서버를 설정해야 할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-210">The server is automatically installed on the on-premises management server, but if you're failing back too much data, you might need to set up an additional master target server.</span></span> <span data-ttu-id="036b3-211">온-프레미스에서 마스터 대상 서버를 설정하려면 다음을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-211">To set up a master target server on-premises, do the following:</span></span>

> [!NOTE]
> <span data-ttu-id="036b3-212">Linux에서 마스터 대상 서버를 설정하려면 다음 절차로 건너뜁니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-212">To set up a master target server on Linux, skip to the next procedure.</span></span> <span data-ttu-id="036b3-213">CentOS 6.6 minimal 운영 체제만 마스터 대상 OS로 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-213">Use only CentOS 6.6 minimal operating system as the master target OS.</span></span>

1. <span data-ttu-id="036b3-214">Windows에서 마스터 대상 서버를 설정하는 경우 마스터 대상 서버를 설치하는 VM에서 빠른 시작 페이지를 엽니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-214">If you're setting up the master target server on Windows, open the quick-start page from the VM that you're installing the master target server on.</span></span>
2. <span data-ttu-id="036b3-215">Azure Site Recovery 통합 설치 마법사의 설치 파일을 다운로드합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-215">Download the installation file for the Azure Site Recovery Unified Setup wizard.</span></span>
3. <span data-ttu-id="036b3-216">설치 마법사를 실행하고 **시작하기 전에**에서 **배포 규모 확장을 위해 추가 프로세스 서버 추가**를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-216">Run the setup and, in **Before you begin**, select **Add additional Process Servers to scale out deployment**.</span></span>
4. <span data-ttu-id="036b3-217">[관리 서버를 설정](site-recovery-vmware-to-azure-classic.md)했을 때 수행한 것과 동일한 방식으로 마법사를 완료합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-217">Complete the wizard in the same way you did when you [set up the management server](site-recovery-vmware-to-azure-classic.md).</span></span> <span data-ttu-id="036b3-218">**구성 서버 세부 정보** 페이지에서 마스터 대상 서버의 IP 주소를 지정하고 VM에 액세스하기 위한 암호를 입력합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-218">On the **Configuration Server Details** page, specify the IP address of the master target server, and enter a passphrase to access the VM.</span></span>

### <a name="set-up-a-linux-vm-as-the-master-target-server"></a><span data-ttu-id="036b3-219">마스터 대상 서버로 Linux VM 설정</span><span class="sxs-lookup"><span data-stu-id="036b3-219">Set up a Linux VM as the master target server</span></span>
<span data-ttu-id="036b3-220">마스터 대상 서버를 실행하는 관리 서버를 Linux VM으로 설정하려면 CentOS 6.6 minimal 운영 체제를 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-220">To set up the management server running the master target server as a Linux VM, install the CentOS 6.6 minimal operating system.</span></span> <span data-ttu-id="036b3-221">다음으로 각 SCSI 하드 디스크의 SCSI ID를 검색하고 몇 가지 추가 패키지를 설치한 다음 일부 사용자 지정 변경 내용을 적용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-221">Next, retrieve the SCSI IDs for each SCSI hard disk, install some additional packages, and apply some custom changes.</span></span>

#### <a name="install-centos-66"></a><span data-ttu-id="036b3-222">CentOS 6.6 설치</span><span class="sxs-lookup"><span data-stu-id="036b3-222">Install CentOS 6.6</span></span>

1. <span data-ttu-id="036b3-223">관리 서버 VM에 CentOS 6.6 최소 운영 체제를 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-223">Install the CentOS 6.6 minimal operating system on the management server VM.</span></span> <span data-ttu-id="036b3-224">DVD 드라이브에 ISO를 보관하고 시스템을 부팅합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-224">Keep the ISO on a DVD drive, and boot the system.</span></span> <span data-ttu-id="036b3-225">미디어 테스트를 건너뜁니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-225">Skip the media testing.</span></span> <span data-ttu-id="036b3-226">언어로 **영어(미국)**를 선택하고, **기본 저장소 장치**를 선택하고, 하드 드라이브에 중요한 데이터가 없는지 확인한 다음 **예**를 클릭하고, 모든 데이터를 삭제합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-226">Select **US English** as the language, select **Basic Storage Devices**, check that the hard drive doesn’t have any important data, click **Yes**, and discard any data.</span></span> <span data-ttu-id="036b3-227">관리 서버의 호스트 이름을 입력하고 서버 네트워크 어댑터를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-227">Enter the host name of the management server, and select the server network adapter.</span></span>  <span data-ttu-id="036b3-228">**시스템 편집** 대화 상자에서 **자동으로 연결**을 선택한 다음 고정 IP 주소, 네트워크 및 DNS 설정을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-228">In the **Editing System** dialog box, select **Connect automatically**, and then add a static IP address, network, and DNS settings.</span></span> <span data-ttu-id="036b3-229">표준 시간대를 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-229">Specify a time zone.</span></span> <span data-ttu-id="036b3-230">관리 서버에 액세스하려면 루트 암호를 입력합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-230">To access the management server, enter the root password.</span></span>
2. <span data-ttu-id="036b3-231">원하는 설치 유형을 묻는 경우 파티션으로 **사용자 지정 레이아웃 만들기**를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-231">When you're asked what type of installation you want, select **Create Custom Layout** as the partition.</span></span> <span data-ttu-id="036b3-232">**다음**을 누릅니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-232">Click **Next**.</span></span> <span data-ttu-id="036b3-233">**무료**를 선택하고 **만들기**를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-233">Select **Free**, and then click **Create**.</span></span> <span data-ttu-id="036b3-234">**FS 유형:** **ext4**를 사용하여 **/**,  **/var/crash** 및 **/home partitions**를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-234">Create **/**,  **/var/crash**, and **/home partitions** with **FS Type:** **ext4**.</span></span> <span data-ttu-id="036b3-235">**FS 형식: 스왑**으로 스왑 파티션을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-235">Create the swap partition as **FS Type: swap**.</span></span>
3. <span data-ttu-id="036b3-236">기존 장치가 발견되면 경고 메시지가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-236">If pre-existing devices are found, a warning message appears.</span></span> <span data-ttu-id="036b3-237">**포맷** 을 클릭하여 파티션 설정으로 드라이브를 포맷합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-237">Click **Format** to format the drive with the partition settings.</span></span> <span data-ttu-id="036b3-238">**디스크에 변경 내용 쓰기** 를 클릭하여 파티션 변경 내용을 적용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-238">Click **Write change to disk** to apply the partition changes.</span></span>
4. <span data-ttu-id="036b3-239">**부팅 로더 설치** > **다음**을 선택하여 루트 파티션에 부팅 로더를 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-239">Select **Install boot loader** > **Next** to install the boot loader on the root partition.</span></span>
5. <span data-ttu-id="036b3-240">설치가 완료되면 **다시 부팅**을 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-240">When the installation is complete, click **Reboot**.</span></span>

#### <a name="retrieve-the-scsi-ids"></a><span data-ttu-id="036b3-241">SCSI ID 검색</span><span class="sxs-lookup"><span data-stu-id="036b3-241">Retrieve the SCSI IDs</span></span>

1. <span data-ttu-id="036b3-242">설치 후 VM의 각 SCSI 하드 디스크에 대한 SCSI ID를 검색합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-242">After the installation, retrieve the SCSI IDs for each SCSI hard disk in the VM.</span></span> <span data-ttu-id="036b3-243">이렇게 하려면 관리 서버 VM을 종료합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-243">To do so, shut down the management server VM.</span></span> <span data-ttu-id="036b3-244">VMware의 VM 속성에서 VM 항목 > **설정 편집** > **옵션**을 마우스 오른쪽 단추로 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-244">In the VM properties in VMware, right-click the VM entry > **Edit Settings** > **Options**.</span></span>
2. <span data-ttu-id="036b3-245">**고급** > **일반 항목**을 선택한 다음 **구성 매개 변수**를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-245">Select **Advanced** > **General item**, and then click **Configuration Parameters**.</span></span> <span data-ttu-id="036b3-246">컴퓨터가 실행 중일 때는 이 옵션을 사용할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-246">This option is unavailable when the machine is running.</span></span> <span data-ttu-id="036b3-247">옵션을 사용하려면 시스템을 종료해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-247">For the option to be available, the machine must be shut down.</span></span>
3. <span data-ttu-id="036b3-248">다음 중 하나를 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-248">Do either of the following:</span></span>
 * <span data-ttu-id="036b3-249">**disk.EnableUUID** 행이 있는 경우 값이 **True**(대/소문자 구분)로 설정되었는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-249">If the row **disk.EnableUUID** exists, make sure that the value is set to **True** (case sensitive).</span></span> <span data-ttu-id="036b3-250">값이 이미 True로 설정되어 있으면 게스트 운영 체제가 부팅된 후 SCSI 명령을 취소하고 테스트할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-250">If the value is already set to True, you can cancel and test the SCSI command inside a guest operating system after it’s booted.</span></span>
 * <span data-ttu-id="036b3-251">**disk.EnableUUID** 행이 없는 경우 **행 추가**를 클릭한 다음 **True** 값을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-251">If the row **disk.EnableUUID** doesn’t exist, click **Add Row**, and then add it with the **True** value.</span></span> <span data-ttu-id="036b3-252">큰따옴표는 사용하지 마세요.</span><span class="sxs-lookup"><span data-stu-id="036b3-252">Don’t use double quotation marks.</span></span>

#### <a name="install-additional-packages"></a><span data-ttu-id="036b3-253">추가 패키지 설치</span><span class="sxs-lookup"><span data-stu-id="036b3-253">Install additional packages</span></span>
<span data-ttu-id="036b3-254">추가 패키지를 다운로드하여 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-254">Download and install additional packages.</span></span>

1. <span data-ttu-id="036b3-255">마스터 대상 서버가 인터넷에 연결되어 있는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-255">Make sure the master target server is connected to the Internet.</span></span>
2. <span data-ttu-id="036b3-256">CentOS 리포지토리에서 15개의 패키지를 다운로드하여 설치하려면 `# yum install –y xfsprogs perl lsscsi rsync wget kexec-tools` 명령을 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-256">To download and install 15 packages from the CentOS repository, run this command: `# yum install –y xfsprogs perl lsscsi rsync wget kexec-tools`.</span></span>
3. <span data-ttu-id="036b3-257">보호하려는 원본 컴퓨터에서 루트 또는 부팅 장치에 대해 Reiser 또는 XFS 파일 시스템으로 Linux를 실행 중인 경우 다음과 같이 추가 패키지를 다운로드하여 설치합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-257">If the source machines you’re protecting are running Linux with a Reiser or XFS file system for the root or boot device, download and install additional packages as follows:</span></span>

   * <span data-ttu-id="036b3-258">\# cd /usr/local</span><span class="sxs-lookup"><span data-stu-id="036b3-258">\# cd /usr/local</span></span>
   * <span data-ttu-id="036b3-259">\# wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm)</span><span class="sxs-lookup"><span data-stu-id="036b3-259">\# wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm)</span></span>
   * <span data-ttu-id="036b3-260">\# wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm)</span><span class="sxs-lookup"><span data-stu-id="036b3-260">\# wget [http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm](http://elrepo.org/linux/elrepo/el6/x86_64/RPMS/reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm)</span></span>
   * <span data-ttu-id="036b3-261">\# rpm –ivh kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm</span><span class="sxs-lookup"><span data-stu-id="036b3-261">\# rpm –ivh kmod-reiserfs-0.0-1.el6.elrepo.x86_64.rpm reiserfs-utils-3.6.21-1.el6.elrepo.x86_64.rpm</span></span>
   * <span data-ttu-id="036b3-262">\# wget [http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm](http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm)</span><span class="sxs-lookup"><span data-stu-id="036b3-262">\# wget [http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm](http://mirror.centos.org/centos/6.6/os/x86_64/Packages/xfsprogs-3.1.1-16.el6.x86_65.rpm)</span></span>
   * <span data-ttu-id="036b3-263">\# rpm –ivh xfsprogs-3.1.1-16.el6.x86_64.rpm</span><span class="sxs-lookup"><span data-stu-id="036b3-263">\# rpm –ivh xfsprogs-3.1.1-16.el6.x86_64.rpm</span></span>
   * <span data-ttu-id="036b3-264">\# yum install device-mapper-multipath(마스터 대상 서버에서 다중 경로 패키지를 사용하도록 설정하는 데 필요함)</span><span class="sxs-lookup"><span data-stu-id="036b3-264">\# yum install device-mapper-multipath (required to enable multipath packages on the master target server)</span></span>

#### <a name="apply-custom-changes"></a><span data-ttu-id="036b3-265">사용자 지정 변경 내용 적용</span><span class="sxs-lookup"><span data-stu-id="036b3-265">Apply custom changes</span></span>
<span data-ttu-id="036b3-266">설치 후 단계를 완료하고 패키지를 설치한 후에는 다음을 수행하여 사용자 지정 변경 내용을 적용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-266">After you’ve completed the post-installation steps and installed the packages, apply custom changes by doing the following:</span></span>

1. <span data-ttu-id="036b3-267">RHEL 6-64 통합 에이전트 바이너리를 VM에 복사합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-267">Copy the RHEL 6-64 Unified Agent binary to the VM.</span></span> <span data-ttu-id="036b3-268">바이너리를 untar하려면 `tar –zxvf <file name>` 명령을 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-268">To untar the binary, run this command: `tar –zxvf <file name>`.</span></span>
2. <span data-ttu-id="036b3-269">권한을 부여하려면 `# chmod 755 ./ApplyCustomChanges.sh` 명령을 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-269">To give permissions, run this command: `# chmod 755 ./ApplyCustomChanges.sh`.</span></span>
3. <span data-ttu-id="036b3-270">`# ./ApplyCustomChanges.sh` 스크립트를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-270">Run the following script: `# ./ApplyCustomChanges.sh`.</span></span> <span data-ttu-id="036b3-271">한 번만 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-271">Run it only once.</span></span> <span data-ttu-id="036b3-272">스크립트가 성공적으로 실행되면 서버를 다시 부팅합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-272">After it runs successfully, reboot the server.</span></span>

## <a name="run-the-failback"></a><span data-ttu-id="036b3-273">장애 복구 실행</span><span class="sxs-lookup"><span data-stu-id="036b3-273">Run the failback</span></span>
### <a name="reprotect-the-azure-vms"></a><span data-ttu-id="036b3-274">Azure VM 다시 보호</span><span class="sxs-lookup"><span data-stu-id="036b3-274">Reprotect the Azure VMs</span></span>
1. <span data-ttu-id="036b3-275">**자격 증명 모음**의 **복제된 항목**에서 장애 조치된 VM을 마우스 오른쪽 단추로 클릭한 다음 **다시 보호**를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-275">In **Vault**, in **Replicated items**, right-click the VM that has been failed over, and then select **Re-Protect**.</span></span>
2. <span data-ttu-id="036b3-276">블레이드에서 보호 방향으로 **Azure - 온-프레미스**가 이미 선택되어 있음을 볼 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-276">On the blade, you can see that the direction of protection **Azure to On-premises** is already selected.</span></span>
3. <span data-ttu-id="036b3-277">**마스터 대상 서버** 및 **프로세스 서버**에서 온-프레미스 마스터 대상 서버 및 Azure VM 프로세스 서버를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-277">In **Master Target Server** and **Process Server**, select the on-premises master target server and the Azure VM Process Server.</span></span>
4. <span data-ttu-id="036b3-278">온-프레미스 디스크를 복구하려는 데이터 저장소를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-278">Select the datastore that you want to recover the disks on-premises to.</span></span> <span data-ttu-id="036b3-279">온-프레미스 VM이 삭제되고 새 디스크를 만들어야 하는 경우에 이 옵션을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-279">Use this option when the on-premises VM is deleted and you need to create new disks.</span></span> <span data-ttu-id="036b3-280">디스크가 이미 있으면 이 옵션을 무시합니다. 다만 값은 지정해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-280">Ignore the option if the disks already exist, but you still need to specify a value.</span></span>
5. <span data-ttu-id="036b3-281">보존 드라이브를 사용하여 VM을 온-프레미스로 다시 복제할 때 특정 시점을 중지합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-281">Use a retention drive to stop the points in time when the VM is replicated back to on-premises.</span></span> <span data-ttu-id="036b3-282">여기서는 보존 드라이브에 대한 몇 가지 기준을 나열합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-282">Some criteria of a retention drive are listed here.</span></span> <span data-ttu-id="036b3-283">이러한 기준이 없으면 드라이브가 마스터 대상 서버에 나열되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-283">Without these criteria, the drive is not listed for the master target server.</span></span>

  * <span data-ttu-id="036b3-284">볼륨은 다른 용도(복제의 대상 등)로 사용되지 않아야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-284">Volume shouldn't be in use for any other purpose (target of replication, and so forth).</span></span>
  * <span data-ttu-id="036b3-285">볼륨은 잠금 모드가 아니어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-285">Volume shouldn't be in lock mode.</span></span>
  * <span data-ttu-id="036b3-286">볼륨은 캐시 볼륨이 아니어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-286">Volume shouldn't be cache volume.</span></span> <span data-ttu-id="036b3-287">(마스터 대상 설치는 해당 볼륨에 없어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-287">(The master target installation shouldn't exist on that volume.</span></span> <span data-ttu-id="036b3-288">프로세스 서버와 마스터 대상 사용자 지정 설치 볼륨은 보존 볼륨에 적합하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-288">The Process Server plus master target custom installation volume is not eligible for retention volume.</span></span> <span data-ttu-id="036b3-289">여기서는 설치된 프로세스 서버와 마스터 대상 볼륨이 마스터 대상의 캐시 볼륨입니다.)</span><span class="sxs-lookup"><span data-stu-id="036b3-289">Here, the installed Process Server plus master target volume is the cache volume of the master target.)</span></span>
  * <span data-ttu-id="036b3-290">볼륨 파일 시스템 유형은 FAT 및 FAT32가 아니어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-290">The volume file system type shouldn't be FAT and FAT32.</span></span>
  * <span data-ttu-id="036b3-291">볼륨 용량은 0이 아닌 값이어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-291">The volume capacity should be non-zero.</span></span>
  * <span data-ttu-id="036b3-292">Windows의 기본 보존 볼륨은 R 볼륨입니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-292">The default retention volume for Windows is R volume.</span></span>
  * <span data-ttu-id="036b3-293">Linux의 기본 보존 볼륨은 /mnt/retention입니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-293">The default retention volume for Linux is /mnt/retention.</span></span>

6. <span data-ttu-id="036b3-294">장애 복구 정책이 자동으로 선택됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-294">The failback policy is automatically selected.</span></span>
7. <span data-ttu-id="036b3-295">**확인**을 클릭하여 다시 보호를 시작하면 Azure에서 온-프레미스 사이트로 VM을 복제하는 작업을 시작합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-295">After you click **OK** to begin reprotection, a job begins to replicate the VM from Azure to the on-premises site.</span></span> <span data-ttu-id="036b3-296">**작업** 탭에서 진행률을 추적할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-296">You can track the progress on the **Jobs** tab.</span></span>

<span data-ttu-id="036b3-297">대체 위치로 복구하려는 경우 마스터 대상 서버에 대해 구성된 보존 드라이브 및 데이터 저장소를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-297">If you want to recover to an alternate location, select the retention drive and datastore that are configured for the master target server.</span></span> <span data-ttu-id="036b3-298">온-프레미스 사이트로 장애 복구할 때 장애 복구 보호 계획의 VMware VM은 마스터 대상 서버와 동일한 데이터 저장소를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-298">When you fail back to the on-premises site, the VMware VMs in the failback protection plan use the same datastore as the master target server.</span></span> <span data-ttu-id="036b3-299">복제본 Azure VM을 동일한 온-프레미스 VM에 복구하려는 경우 온-프레미스 VM은 마스터 대상 서버와 동일한 데이터 저장소에 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-299">If you want to recover the replica Azure VM to the same on-premises VM, the on-premises VM should already be in the same datastore as the master target server.</span></span> <span data-ttu-id="036b3-300">온-프레미스에 VM이 없는 경우 다시 보호하는 동안 새 VM이 만들어집니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-300">If there's no VM on-premises, a new one is created during reprotection.</span></span>

![드롭다운 메뉴에서 "Azure - 온-프레미스"를 선택합니다.](./media/site-recovery-failback-azure-to-vmware-new/reprotectinputs.png)

<span data-ttu-id="036b3-302">복구 계획 수준에서 다시 보호할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-302">You can also reprotect at a recovery plan level.</span></span> <span data-ttu-id="036b3-303">복제 그룹이 있는 경우 복구 계획을 통해서만 이 복제 그룹을 다시 보호할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-303">If you have a replication group, you can reprotect it only by using a recovery plan.</span></span> <span data-ttu-id="036b3-304">복구 계획을 사용하여 다시 보호하는 경우 모든 보호 대상 컴퓨터에 대해 이전 값을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-304">When you reprotect by using a recovery plan, use the previous values for every protected machine.</span></span>

> [!NOTE]
> <span data-ttu-id="036b3-305">동일한 마스터 대상 서버를 사용하여 복제 그룹을 다시 보호해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-305">Replication groups should be protected back with the same master target.</span></span> <span data-ttu-id="036b3-306">다른 마스터 대상 서버를 통해 복제 그룹을 다시 보호하는 경우 이 복제 그룹에 공통된 특정 시점을 결정할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-306">If they are protected back with different master target servers, a common point in time cannot be determined for them.</span></span>

### <a name="run-a-failover-to-the-on-premises-site"></a><span data-ttu-id="036b3-307">온-프레미스 사이트로 장애 조치 실행</span><span class="sxs-lookup"><span data-stu-id="036b3-307">Run a failover to the on-premises site</span></span>
<span data-ttu-id="036b3-308">VM을 다시 보호한 후에는 Azure에서 온-프레미스로 장애 조치를 시작할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-308">After you reprotect the VM, you can initiate a failover from Azure to on-premises.</span></span>

1. <span data-ttu-id="036b3-309">복제된 항목 페이지에서 가상 컴퓨터를 마우스 오른쪽 단추로 클릭한 다음 **계획되지 않은 장애 조치**를 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-309">On the replicated items page, right-click the virtual machine, and then select **Unplanned Failover**.</span></span>
2. <span data-ttu-id="036b3-310">**장애 조치(failover) 확인** 에서 장애 조치 방향(Azure에서)을 확인한 후 장애 조치에 사용할 복구 지점(최신 시점 또는 최신 응용 프로그램 일치 복구 지점)을 선택합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-310">In **Confirm Failover**, verify the failover direction (from Azure), and then select the recovery point that you want to use for the failover (the latest, or the latest app-consistent recovery point).</span></span> <span data-ttu-id="036b3-311">최신 시점 이전에 최신 응용 프로그램 일치 복구 지점이 발생하고 일부 데이터가 손실됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-311">An app-consistent recovery point occurs before the most recent point in time, and it will cause some data loss.</span></span>
3. <span data-ttu-id="036b3-312">장애 조치 중에 Site Recovery에서 Azure VM을 종료합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-312">During failover, Site Recovery shuts down the Azure VMs.</span></span> <span data-ttu-id="036b3-313">장애 복구가 예상대로 완료되었는지 확인한 후에 Azure VM이 예상대로 종료되었는지 확인할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-313">After you check that failback has been completed as expected, you can check to ensure that the Azure VMs have been shut down as expected.</span></span>

### <a name="reprotect-the-on-premises-site"></a><span data-ttu-id="036b3-314">온-프레미스 사이트 다시 보호</span><span class="sxs-lookup"><span data-stu-id="036b3-314">Reprotect the on-premises site</span></span>
<span data-ttu-id="036b3-315">장애 복구가 완료된 후 가상 컴퓨터를 커밋하여 Azure에서 복구된 VM이 삭제되었는지 확인해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-315">After failback has been completed, commit the virtual machine to ensure that the VMs recovered in Azure are deleted.</span></span> <span data-ttu-id="036b3-316">이렇게 하려면 보호된 항목을 마우스 오른쪽 단추로 클릭한 다음 **커밋**을 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-316">To do so, right-click the protected item, and then click **Commit**.</span></span> <span data-ttu-id="036b3-317">이 작업은 Azure에서 이전에 복구된 가상 컴퓨터를 제거하는 작업을 트리거합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-317">This action triggers a job that removes the former recovered virtual machines in Azure.</span></span>

<span data-ttu-id="036b3-318">커밋이 완료되면 데이터가 온-프레미스 사이트에 다시 있어야 하지만 보호되지는 않습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-318">After the commit is completed, your data should be back on the on-premises site, but it won’t be protected.</span></span> <span data-ttu-id="036b3-319">Azure로 다시 복제하려면 다음을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-319">To start replicating to Azure again, do the following:</span></span>

1. <span data-ttu-id="036b3-320">**자격 증명 모음**의 **설정** > **복제된 항목**에서 장애 복구한 VM을 선택한 다음 **다시 보호**를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-320">In **Vault**, in **Setting** > **Replicated items**, select the VMs that have failed back, and then click **Re-Protect**.</span></span>
2. <span data-ttu-id="036b3-321">Azure로 데이터를 다시 전송하는 데 사용해야 하는 프로세스 서버의 값을 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-321">Give the value of the Process Server that needs to be used to send data back to Azure.</span></span>
3. <span data-ttu-id="036b3-322">**확인**을 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-322">Click **OK**.</span></span>

<span data-ttu-id="036b3-323">다시 보호가 완료되면 VM이 Azure로 다시 복제하고 장애 조치를 수행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-323">After the reprotection is complete, the VM replicates back to Azure and you can do a failover.</span></span>

### <a name="resolve-common-failback-issues"></a><span data-ttu-id="036b3-324">일반적인 장애 복구 문제 해결</span><span class="sxs-lookup"><span data-stu-id="036b3-324">Resolve common failback issues</span></span>
* <span data-ttu-id="036b3-325">읽기 전용 사용자 vCenter 검색을 수행하고 가상 컴퓨터를 보호하면 작업에 성공하고 장애 조치가 작동합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-325">If you perform a read-only user vCenter discovery and protect virtual machines, it succeeds and failover works.</span></span> <span data-ttu-id="036b3-326">다시 보호 중에는 데이터 저장소를 검색할 수 없기 때문에 장애 조치가 실패합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-326">During reprotection, failover fails because the datastores cannot be discovered.</span></span> <span data-ttu-id="036b3-327">하나의 증상으로, 다시 보호하는 동안 나열된 데이터 저장소가 표시되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-327">As a symptom, you will not see the datastores listed during reprotection.</span></span> <span data-ttu-id="036b3-328">이 문제를 해결하려면 vCenter 자격 증명을 권한이 있는 적절한 계정으로 업데이트하고 작업을 다시 시도하면 됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-328">To resolve this issue, you can update the vCenter credential with an appropriate account that has permissions and retry the job.</span></span> <span data-ttu-id="036b3-329">자세한 내용은 [Azure Site Recovery를 사용하여 Azure에 VMware 가상 컴퓨터 및 물리적 서버 복제](site-recovery-vmware-to-azure-classic.md)를 참조하세요.</span><span class="sxs-lookup"><span data-stu-id="036b3-329">For more information, see [Replicate VMware virtual machines and physical servers to Azure with Azure Site Recovery](site-recovery-vmware-to-azure-classic.md)</span></span>
* <span data-ttu-id="036b3-330">Linux VM을 장애 복구하고 이 VM을 온-프레미스에서 실행하면 네트워크 관리자 패키지가 컴퓨터에서 제거되었음을 알 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-330">When you fail back a Linux VM and run it on-premises, you can see that the Network Manager package has been uninstalled from the machine.</span></span> <span data-ttu-id="036b3-331">이 제거는 Azure에서 VM을 복구할 때 네트워크 관리자 패키지가 제거되었기 때문에 발생합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-331">This uninstallation happens because the Network Manager package is removed when the VM is recovered in Azure.</span></span>
* <span data-ttu-id="036b3-332">VM을 고정 IP 주소로 구성하고 Azure로 장애 조치하면 DHCP를 통해 IP 주소를 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-332">When a VM is configured with a static IP address and is failed over to Azure, the IP address is acquired via DHCP.</span></span> <span data-ttu-id="036b3-333">온-프레미스로 다시 장애 조치하면 VM에서 DHCP를 사용하여 IP 주소를 계속 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-333">When you fail over back to on-premises, the VM continues to use DHCP to acquire the IP address.</span></span> <span data-ttu-id="036b3-334">컴퓨터에 수동으로 로그인하고, 필요한 경우 IP 주소를 고정 주소로 다시 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-334">Manually sign in to the machine and set the IP address back to a static address if necessary.</span></span>
* <span data-ttu-id="036b3-335">ESXi 5.5 무료 버전 또는 vSphere 6 하이퍼바이저 무료 버전을 사용하는 경우 장애 조치는 성공하지만 장애 복구가 실패합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-335">If you are using either ESXi 5.5 free edition or vSphere 6 Hypervisor free edition, failover would succeed, but failback would not succeed.</span></span> <span data-ttu-id="036b3-336">장애 복구를 사용하도록 설정하려면 평가판 라이선스로 업그레이드합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-336">To enable failback, upgrade to either program's evaluation license.</span></span>
* <span data-ttu-id="036b3-337">프로세스 서버에서 구성 서버에 연결할 수 없는 경우 443 포트의 구성 서버 컴퓨터에 텔넷하여 구성 서버에 대한 연결성을 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-337">If you cannot reach the configuration server from the Process Server, check connectivity to the configuration server by Telnet to the configuration server machine on port 443.</span></span> <span data-ttu-id="036b3-338">프로세스 서버 컴퓨터에서 구성 서버에 대한 ping을 시도할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-338">You can also try to ping the configuration server from the Process Server machine.</span></span> <span data-ttu-id="036b3-339">또한 프로세스 서버는 구성 서버에 연결될 때 하트비트가 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-339">A Process Server should also have a heartbeat when it is connected to the configuration server.</span></span>
* <span data-ttu-id="036b3-340">대체 vCenter로 장애 복구하려고 시도하는 경우 새 vCenter가 검색되고 마스터 대상 서버도 검색되는지 확인해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-340">If you are trying to fail back to an alternate vCenter, make sure that your new vCenter is discovered and that the master target server is also discovered.</span></span> <span data-ttu-id="036b3-341">일반적인 증상으로 **다시 보호** 대화 상자에서 데이터 저장소에 액세스할 수 없거나 데이터 저장소를 볼 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-341">A typical symptom is that the datastores are not accessible or visible in the **Reprotect** dialog box.</span></span>
* <span data-ttu-id="036b3-342">물리적 온-프레미스 컴퓨터로 보호되는 WS2008R2SP1 컴퓨터는 Azure에서 온-프레미스로 장애 복구할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-342">A WS2008R2SP1 machine that is protected as a physical on-premises machine cannot be failed back from Azure to on-premises.</span></span>

## <a name="fail-back-with-expressroute"></a><span data-ttu-id="036b3-343">ExpressRoute로 장애 복구</span><span class="sxs-lookup"><span data-stu-id="036b3-343">Fail back with ExpressRoute</span></span>
<span data-ttu-id="036b3-344">VPN 연결 또는 Azure ExpressRoute 연결을 통해 장애 복구할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-344">You can fail back over a VPN connection or by using an ExpressRoute connection.</span></span> <span data-ttu-id="036b3-345">ExpressRoute 연결을 사용하려면 다음에 유의합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-345">If you want to use an ExpressRoute connection, note the following:</span></span>

* <span data-ttu-id="036b3-346">ExpressRoute 연결은 원본 컴퓨터에서 장애 조치하고 장애 조치가 발생한 후 Azure VM이 있는 Azure 가상 네트워크에 설정되어 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-346">The ExpressRoute connection should be set up on the Azure virtual network that the source machines fail over to and where the Azure VMs are located after the failover occurs.</span></span>
* <span data-ttu-id="036b3-347">데이터는 공용 끝점의 Azure 저장소 계정에 복제됩니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-347">Data is replicated to an Azure storage account on a public endpoint.</span></span> <span data-ttu-id="036b3-348">ExpressRoute 연결을 사용하려면 ExpressRoute에서 Site Recovery 복제를 위한 대상 데이터 센터로 공용 피어링을 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="036b3-348">To use an ExpressRoute connection, set up public peering in ExpressRoute with the target data center for Site Recovery replication.</span></span>
